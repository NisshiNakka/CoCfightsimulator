<%= turbo_stream.update "dice_result" do %>
  <div class="alert <%= @success ? 'alert-success' : 'alert-danger' %> result-box">
    <h4>【<%= @status %>】</h4>
    <p><%= @result_text %></p>

    <!--
    <h3>ロール結果</h3>
    <p class="result-text"><%= @result_text %></p>
    
    <% if @critical %>
      <span class="badge bg-warning">クリティカル！</span>
    <% elsif @fumble %>
      <span class="badge bg-danger">ファンブル！</span>
    <% elsif @regular %>
      <span class="badge bg-success">成功</span>
    <% elsif @hard %>
      <span class="badge bg-info">ハード成功</span>
    <% elsif @extreme %>
      <span class="badge bg-purple">イクストリーム</span>
    <% else %>
      <span class="badge bg-danger">失敗</span>
    <% end %>
    -->
  </div>
<% end %>